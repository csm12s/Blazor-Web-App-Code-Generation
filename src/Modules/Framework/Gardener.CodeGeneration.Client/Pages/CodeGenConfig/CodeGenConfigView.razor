@*@page "/system_tool/code_gen_config"*@
@inherits ListTableBase<CodeGenConfigDto, int>

<Table @ref="_table"
       TItem="CodeGenConfigDto"
       DataSource="@_datas"
       Total="_total"
       OnChange="@OnChange"
       @bind-PageIndex="_pageIndex"
       @bind-PageSize="_pageSize"
       @bind-SelectedRows="_selectedRows"
       Loading="@_tableIsLoading"
       Context="model"
       RemoteDataSource>
    <TitleTemplate>
        <TableSearch TDto="CodeGenConfigDto" OnSearchFieldChanged="SetPresetFilterGroups" OnSearch="()=>ReLoadTable(true)" DefaultValue="_defaultSearchValue"></TableSearch>
        <Row>
            <AntDesign.Col Span="8">
                <Button Type="@ButtonType.Primary" Icon="delete" Danger OnClick="OnClickSaveAll" Loading="_saveAllBtnLoading">
                    @localizer["SaveAll"]
                </Button>
            </AntDesign.Col>
            <AntDesign.Col Span="16" Style="text-align:right">
                <Space>
                    <SpaceItem>
                        <Button Type="@ButtonType.Primary" Icon="reload" OnClick="ReLoadTable">
                            @localizer["刷新"]
                        </Button>
                    </SpaceItem>
                </Space>
            </AntDesign.Col>
        </Row>
    </TitleTemplate>
    <RowTemplate>
        <Selection Key="@(model.Id.ToString())" />
        @*<Column @bind-Field="@model.Id" Sortable />*@
        <Column @bind-Field="@model.ColumnName" Width="100" Filterable />
        <Column @bind-Field="@model.ColumnDescription" Width="100" Filterable />
        <Column @bind-Field="@model.DbDataType" Width="100" Filterable />
        <Column @bind-Field="@model.DataType" Width="100" Filterable />
        <Column @bind-Field="@model.NetType" Width="100" Filterable />
        <Checkbox @bind-Checked="@model.IsSearch" Width="30" />
        <Checkbox @bind-Checked="@model.IsView" Width="30" />
        <Checkbox @bind-Checked="@model.IsCreate" Width="30" />
        <Checkbox @bind-Checked="@model.IsEdit" Width="30" />
        <Checkbox @bind-Checked="@model.IsBatchEdit" Width="30" />
        <Column @bind-Field="@model.CreatedTime" Format="@ClientConstant.DateTimeFormat" Sortable DefaultSortOrder="@SortDirection.Descending" Width="100" />
        <Column @bind-Field="@model.UpdatedTime" Format="@ClientConstant.DateTimeFormat" Sortable DefaultSortOrder="@SortDirection.Descending" Width="100" />
        @*<ActionColumn>
        <Space>
        <SpaceItem>
        <Button Type="@ButtonType.Link">
        @localizer["Edit"]
        </Button>
        </SpaceItem>
        </Space>
        </ActionColumn>*@
    </RowTemplate>
</Table>
