@*@inherits RazorEngine.Templating.TemplateBase<Gardener.CodeGeneration.Dtos.CodeGenNameModel>*@

using Furion.DatabaseAccessor;
using Furion.DependencyInjection;
using @(Model.AppName).@(Model.Module).Server.Model;
using @(Model.AppName).Sugar;

namespace @(Model.AppName).@(Model.Module).Server.Service;

public class @(Model.ClassName)Service : @(Model.Module)BaseService<@(Model.ClassName)>,
    ITransient
    @*I@(Model.ClassName)Service*@
{
    public @(Model.ClassName)Service(IRepository<@(Model.ClassName), MasterDbContextLocator> repository,
        SqlSugarRepository<@(Model.ClassName)> sugarRepository) 
        : base(repository, sugarRepository)
    {
    }
}
