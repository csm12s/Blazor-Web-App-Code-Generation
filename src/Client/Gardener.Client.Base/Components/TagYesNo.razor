<Tag Color="@(Yes?"blue":"red")">
    @(Yes ? localizer["yes"]: localizer["no"])
</Tag>
@code {
    [Inject]
    protected IClientLocalizer localizer { get; set; }
    [Parameter]
    public bool Yes { get; set; }
}
